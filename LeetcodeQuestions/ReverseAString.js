var reverseString = function(s){


    //made a variable and stored the reverse string 
    var revArray = s.reverse();
    //console logging to check the answer
    console.log(revArray);

    // returning the revarray answer
    return revArray;
}
reverseString(['M','R','I','N','A','L',]);